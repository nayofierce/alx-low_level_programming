#include <stdio.h>

/**
 *main - Entry point
 *
 * Return: always 0 (Success)
 */
int main(void)
{
char letter;
for ((letter = 'a'); (letter = 'z'); letter++)
if (letter == 'q') /*continue if is q*/
{
continue;
}
if (letter == 'e') /*continue if is q*/
{
continue;
}
{
putchar(letter);
putchar('\n');
}
return (0);
}
