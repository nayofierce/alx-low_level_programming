#include "main.h"

/**
 * _isupper - Single line, short desription
 * @c: contains value to be compared
 * Return: 1, 0
 */

int _isupper(int c)
{
if (c > 'A' && c < 'Z')
{
return (1);
}
else
{
return (0);
}
